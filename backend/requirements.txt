# Versões fixadas nas que já rodam os 161 testes automatizados nesta máquina (`pip freeze`)
# -- antes disso o arquivo não fixava nenhuma versão, então `pip install` hoje e daqui uns
# meses podiam resolver pacotes completamente diferentes (reprodutibilidade frágil).
fastapi==0.139.0
uvicorn==0.51.0
sqlalchemy==2.0.51
motor==3.7.1
certifi==2026.6.17
pydantic==2.13.4
pydantic[email]==2.13.4
pydantic-settings==2.14.2
python-jose[cryptography]==3.5.0
passlib==1.7.4
python-multipart==0.0.32
alembic==1.18.5

# Produção (Postgres real, ver docs/BACKEND.md) -- sem versão fixa de propósito: não está
# instalado neste ambiente de dev (só SQLite é usado aqui), então não há uma versão "provada"
# nesta máquina pra fixar com confiança. Fixar assim que for instalado contra um Postgres real.
psycopg2-binary

# Só para testes (tests/test_backend_bitins.py) -- não usados em produção:
mongomock-motor==0.0.36
httpx==0.28.1

# Lint (2026-07-16) -- primeiro linter Python do projeto, ver pyproject.toml na raiz pra config.
ruff==0.15.21
