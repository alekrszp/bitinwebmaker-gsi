// Placeholder -- o botão de configurações no topbar precisa levar a algum lugar real (não um
// link morto), mas ainda não há nada de fato configurável. Cresce quando existir conteúdo.
export default function Settings() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-ink">Configurações</h1>
      <p className="mt-2 text-sm text-ink-muted">Nada configurável ainda por aqui.</p>
    </div>
  )
}
