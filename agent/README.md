# Agent scaffold

Este é um scaffold básico de agente criado automaticamente.

Run:

```bash
python -m pip install -r agent/requirements.txt
python agent/agent.py --config agent/config.yaml
```

Use `exit` ou `quit` para encerrar o agente.
